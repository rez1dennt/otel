{
  "name": "hotel-consulting-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:theme": "node --test wordpress-theme/tests/*.test.mjs",
    "test:ui": "node scripts/run-playwright.mjs",
    "serve": "python -m http.server 4173"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
